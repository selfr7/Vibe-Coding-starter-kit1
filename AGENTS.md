# AGENTS.md — AI Development Rules

你是这个项目的 AI Coding Agent。

在修改项目之前，先理解现有代码、文档和项目目标。不要为了完成一个小需求而重写无关代码。

## 1. 工作方式

- 先阅读相关文件，再修改。
- 不要假设项目不存在现有约定。
- 对较大的任务先给出计划，再实施。
- 尽量小范围、可回滚地修改。
- 不要无理由重构整个项目。
- 不要为了“看起来更专业”而增加不必要的依赖或架构。
- 如果发现需求存在明显冲突，先指出冲突。
- 项目内置 Vite + React + Tailwind 最小骨架。任何改动完成后，项目必须仍能运行：`npm run dev` 可启动、`npm run build` 能通过。

## 2. 产品目标

以 `docs/PROJECT.md` 为产品事实来源之一。

如果实现与项目目标冲突，应优先提醒用户，而不是擅自扩展产品范围。

## 3. UI / UX

- 页面必须考虑移动端和桌面端。
- 优先使用语义化 HTML。
- 保持清晰的信息层级。
- 保持间距、字体、按钮、表单等组件的一致性。
- 交互应有明确的 loading、empty、error 和 success 状态。
- 关注键盘操作、对比度、可读性和基本无障碍体验。
- 不要为了炫技加入无必要的动画。

## 4. Design

以 `docs/DESIGN.md` 为视觉规则来源。

- 优先建立一致的设计系统。
- 不要复制现有商业网站。
- 不要刻意制作与特定品牌高度相似的 Logo 或视觉身份。
- 如果用户要求模仿具体品牌，应改为描述通用的设计特征，而不是复制具体资产。

## 5. Assets / IP

- 不要从搜索引擎随机下载图片用于商业项目。
- 不要随意复制第三方网站的图片、SVG、文字、Logo 或其他资产。
- 不要把不明来源的图片或资源直接放进 `public/`。
- 第三方资源必须尽量有明确来源和 License。
- 使用第三方资源后，在 `ASSET_LICENSE.md` 中记录。
- 不确定授权时，优先不用。
- Logo 和品牌名称在商业上线前建议进行商标/IP 检查。

## 6. Fonts

使用字体前确认其许可证是否允许项目所需的商业使用方式。

不要因为“可以免费下载”就假设“可以任意商业使用”。

## 7. Icons

优先使用项目已采用、具有明确许可证的 Icon Library，或自行制作 SVG。

不要随机从互联网下载未知来源的 SVG。

## 8. Dependencies

添加 npm/package 依赖前：

1. 确认现有项目是否已经有类似能力。
2. 判断是否真的需要新依赖。
3. 尽量选择维护良好、许可证清晰的依赖。
4. 对特殊许可证保持警惕，并提醒用户。
5. 不要为了一个很小的功能安装大型依赖。

## 9. Security

- 不要把 API Key、密码、Token、数据库连接密钥写进源码。
- 不要把 `.env` / `.env.local` 等秘密文件提交到 Git。
- 环境变量只把变量名写进 `.env.example`（不放真实值）；新增环境变量时同步更新它，真实值让用户填在 `.env.local`。
- 不要在前端暴露应该保密的服务器密钥。
- 涉及身份认证、支付、数据库权限时，不要假设客户端校验足够。

## 10. Code Quality

- 优先简单、清晰、可维护的实现。
- 避免过早抽象。
- 避免重复代码，但不要为了消除几行重复代码而制造复杂架构。
- 保持组件职责清晰。
- 修改后检查相关 lint/type/test/build 问题（如果项目配置了这些工具）。

## 11. Before Completing a Task

在告诉用户“完成”之前：

- 检查修改是否真的满足需求。
- 检查是否引入明显错误。
- 确认项目仍能运行（`npm run dev` 可启动、`npm run build` 能通过）。
- 检查相关页面的响应式表现。
- 检查是否产生了不必要的新依赖。
- 如果加入了第三方素材，更新 `ASSET_LICENSE.md`。
- 在 `docs/CHANGELOG.md` 最上方追加一行：`日期 | 做了什么 | 动了哪些文件`。
- 简明说明改了什么，以及是否还有已知限制。