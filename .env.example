# 环境变量清单（只放变量名和说明，绝不放真实密钥）
#
# 用法（全程让 AI 代劳，你不用动文件）：
# 1. 需要密钥时，告诉 AI「帮我创建 .env.local，我要填 XX 的密钥」
# 2. AI 会复制本文件为 .env.local，并引导你把真实值填进去（.env.local 不会被上传）
# 3. AI 新增变量时，会把变量名同步加到本文件
#
# 注意：Vite 项目里，前端能用到的变量必须以 VITE_ 开头

# 示例（用到时再取消注释并填值）：
# VITE_API_KEY=你的密钥填在这里